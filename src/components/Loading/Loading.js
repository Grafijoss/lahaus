import { Animation, LoadingContainer } from './Styles'

const Loading = () => {
    return (
        <LoadingContainer>
            <Animation>Cargando</Animation>
        </LoadingContainer>
    )
}

export default Loading;